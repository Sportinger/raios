# Device Protocol

Specifications and tooling for the WebSocket JSON envelope `{v,t,id,ts,body}` shared between the device and fake cloud. Will include schemas, validators, and docs per runbook section 2.3.
