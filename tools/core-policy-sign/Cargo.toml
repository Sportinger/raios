[package]
name = "core-policy-sign"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
p256 = { version = "0.13.2", features = ["ecdsa"] }
raios-core = { path = "../../crates/raios-core" }
rand = { version = "0.8", features = ["std"] }
