[package]
name = "descriptor-resign"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
hex = "0.4"
p256 = { version = "0.13.2", features = ["ecdsa"] }
rand = { version = "0.8", features = ["std"] }
