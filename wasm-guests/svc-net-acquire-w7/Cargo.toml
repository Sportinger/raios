[package]
name = "svc-net-acquire-w7"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
raios-http-parse = { path = "../../raios-http-parse" }
raios-w7-acquire-logic = { path = "../../raios-w7-acquire-logic" }
raios-x509-spki = { path = "../../raios-x509-spki" }

